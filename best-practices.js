"use strict";

module.exports = {

  "rules": {

    /* Don't care if some returns return no value */
    // "consistent-return": 0,

    /* Require === and !== */
    "eqeqeq": "error",

    "no-alert": "error",
    "no-caller": "error",

    /* Disallow captialized function to be used without new. */
    // "new-cap": 0,

    "no-else-return": "error",
    "no-fallthrough": "error",
    "no-floating-decimal": "error",

    /* Don't care if there's more than one space anywhere. */
    "no-multi-spaces": "error",

    "no-redeclare": "error",

    /* Don't care if there are unused experssions. eg "flag && doThing();" */
    // "no-unused-expressions": 0,

    "no-with": "error"

  }

};
