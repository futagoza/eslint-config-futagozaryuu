branches:
  only:
    - "master"

language: node_js

node_js:
  - "6"
  - "8"
  - "10"

cache:
  directories:
    - node_modules
  yarn: true

install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash
  - export PATH=$HOME/.yarn/bin:$PATH
  - yarn --ignore-engines
