{
    "name": "eslint-config-futagozaryuu",
    "private": true,
    "workspaces": [
        "packages/@futagoza/*"
    ],
    "scripts": {
        "fix": "eslint . --fix",
        "postversion": "git push --follow-tags && npm publish",
        "test": "eslint ."
    },
    "devDependencies": {
        "eslint": "~6.0.1"
    },
    "engines": {
        "node": ">= 8"
    }
}
